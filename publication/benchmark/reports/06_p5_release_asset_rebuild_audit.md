# P5 Release Asset Rebuild Audit

**Status: PASS (candidate)**

`DLG_GNN_Benchmark_v1.0.0_preprint.zip` was rebuilt from P5 repository truth. It contains the corrected README, canonical artifacts and provenance, exact runner dependencies, and release metadata. Mode 1 and the static production-runner dry-run pass from clean unpack. Final SHA-256 is recorded after the release commit is frozen.
