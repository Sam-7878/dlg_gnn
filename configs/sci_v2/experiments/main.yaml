dataset:
  version: gog-sci-v2.0
  canonical_root: /mnt/d/_Work/_data/GoG_sci_v2
  physical_transaction_root: /mnt/d/_Work/_data/dataset/transactions
  labels_path: /mnt/d/_Work/_data/dataset/labels.csv
  chains: [ethereum, bsc, polygon]
experiment:
  name: dlg_streammc_sci_v2_main
  output_root: results_sci_v2
  seeds: [11, 22, 33, 44, 55]
gates:
  require_clean_git: true
  require_leakage_pass: true
  require_v2_internal_mapping: true
  legacy_compatibility: informational
  prohibit_demo_metrics: true
# Deliberately empty until the full-data P0 gate passes. Strict orchestration
# must fail closed rather than silently execute a demonstration path.
pipeline_commands: []
