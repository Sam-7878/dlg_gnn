# configs/mc/static_benchmark.yaml
dataset:
  transactions_root: "../_data/dataset/transactions"
  labels_path: "../_data/dataset/labels.csv"
  global_graph_root: "../_data/dataset/global_graph"
  chain: "polygon"
  auto_split: true
  train_ratio: 0.7
  val_ratio: 0.15
  load_global_graph: true

level1:
  encoder_backend: "gnn"
  in_dim: 3
  out_dim: 1
  epochs: 5
  batch_size: 128
  lr: 0.001

level2:
  hidden_dim: 128
  epochs: 5
  batch_size: 128
  lr: 0.001

mc_samples: 8
dropout_p: 0.10

setting: "mc_static"
output_dir: "results/benchmark_mc_static"
